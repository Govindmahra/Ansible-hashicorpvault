---
plugin: aws_ec2
regions:
  - us-east-1  # Replace with your AWS region
    #filters:
    #instance-state-name: running
    #tag:Name: vault
keyed_groups:
  - key: tags.Environment
    prefix: tag_Environment__
    separator: ""
hostnames:
  - private-ip-address
compose:
  ansible_host: private_ip_address
  ansible_user: ubuntu
~                      
